<div class="container">
  <div class="row">
    <div class="col-sm-2 my-4"></div>
    <div class="col-sm-3 mt- my-4">
      <h3>商品一覧<span>（全<%= @items.count %>件）</span></h3>
    </div>
    <div class="col-sm-7 my-4">
      <p>
        ※ジャンル検索の場合、「商品一覧」の部分は「<strong>（ジャンル名）</strong>」に変更する<br>
        <例>「ケーキ一覧」
      </p>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-2 my-4"></div>
    <div class="col-sm-10 my-4">
      <% @items.last(8).each do |item| %>
        <div class="item-box">
          <%= link_to item_path(item.id) do %>
            <%= attachment_image_tag item, :image, format: 'jpeg', size:'100x100' %>
            <div class="items-body">
              <h5><%= item.name %></h5>
              <p><%= item.add_tax_price.to_s(:delimited) %>円</p>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>

  </div>
  <div class="text-center">
    <%= paginate @items %>
  </div>
</div>